export default {
  summation: (a, b) => a + b,
  division: (a, b) => a / b,
  multiplication: (a, b) => a * b,
  exponent: (a, b) => Math.pow(a, b),
  subtraction: (a, b) => a - b,
  bitwiseLeft: (a, b) => a >> b,
  bitwiseRight: (a, b) => a << b,
};
